# Force devDependencies to install even where the user/global npm config
# sets omit=dev (that setting silently skipped vitest installs and hid
# dev deps from npm ls — a documented recurring hazard). 2026-06-11.
include=dev
