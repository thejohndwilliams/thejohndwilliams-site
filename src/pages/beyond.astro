---
import BaseLayout from '../layouts/BaseLayout.astro';
import KineticPlate from '../components/KineticPlate.astro';
import KineticPlate3D from '../components/KineticPlate3D.astro';
import LiquidPlate from '../components/LiquidPlate.astro';
import ReliefStudies from '../components/ReliefStudies.astro';

// LABS (owner directive 2026-07-31): one home for the photography
// experiments. Merges the depth/hologram comparison work (from the private
// lab, which keeps its production gate and P1 lock) with the relief - the
// object axis - discussed in more depth than the work page's resume view.
// Work stays the living resume of domains; Labs is where the experiments
// live and are explained.
const ROWS = [
  { id: 'earth', label: 'Earth · rain-wet leaf', note: 'A dark subject. Luminance buries it; depth stands it forward.' },
  { id: 'structure', label: 'Structure · Shanghai Tower', note: 'Shot against bright sky. Depth keeps the tower near, the sky far.' },
  { id: 'sky', label: 'Sky · thunderheads', note: 'The nearest billows read closest; the void recedes to black.' },
  { id: 'water', label: 'Water · El Arco', note: 'A distant scene. The arch holds its place in real space.' },
] as const;
---

<BaseLayout
  title="Beyond"
  description="Beyond the frame: photographs rebuilt as holograms and true point clouds, textures and Gaussian studies to take home, and images cut as physical bas-relief. The experiments of John D. Williams."
  ogImage="/og/photography/gv-3b61b429.jpg"
>
  {/* HERO - the house pattern: banner, verb, name. The verb liturgy runs
      making visible / noticing / building / rising; this wing's act is
      LIFTING - its own inherited sentence is "Lift each experience from
      its frame." Banner: the last light beyond the ridge (gv-3b61b429),
      a threshold in the dark register. */}
  <section class="relative min-h-[50vh] sm:min-h-[60vh] md:min-h-[80vh] flex items-end overflow-hidden">
    <picture>
      <source
        type="image/avif"
        srcset="/images/photography/thumb/gv-3b61b429.avif 400w, /images/photography/gallery/gv-3b61b429.avif 1200w, /images/photography/hero/gv-3b61b429.avif 2400w"
        sizes="100vw"
      />
      <source
        type="image/webp"
        srcset="/images/photography/thumb/gv-3b61b429.webp 400w, /images/photography/gallery/gv-3b61b429.webp 1200w, /images/photography/hero/gv-3b61b429.webp 2400w"
        sizes="100vw"
      />
      <img
        src="/images/photography/hero/gv-3b61b429.webp"
        alt="A lone tree in silhouette against the last light of sunset beyond a dark ridge"
        class="absolute inset-0 w-full h-full object-cover"
        fetchpriority="high"
      />
    </picture>
    <div class="absolute inset-0 bg-gradient-to-t from-[#0a0a0a] via-[#0a0a0a]/40 to-transparent"></div>
    <div class="hero-lockup relative z-10 w-full px-6 sm:px-10 lg:px-20 pb-10 sm:pb-16 md:pb-24">
      <div class="reveal max-w-2xl">
        <p class="eyebrow mb-3 sm:mb-4">Lifting</p>
        <h1 class="t-title font-serif mb-4 sm:mb-6 tracking-tight">Beyond</h1>
      </div>
    </div>
  </section>

  {/* STATEMENT - one block, then three acts: depth, texture, object. */}
  <section class="py-12 sm:py-16 md:py-20">
    <div class="container-narrow">
      <div class="prose max-w-2xl">
        <p>
          Experiments in what a photograph can become once it leaves the
          frame. Each one starts the same way: a finished photograph, taken
          seriously enough to ask what else it holds. Depth, first. Then
          texture. Then surface.
        </p>
      </div>
    </div>
  </section>

  {/* ACT I - THE DEPTH */}
  <section class="pt-4 pb-6 sm:pb-8">
    <div class="container-narrow">
      <h2 class="t-sub font-serif mb-3 sm:mb-4">The depth studies</h2>
      <p class="text-sm sm:text-base opacity-55 max-w-2xl mb-2">
        The same photographs, twice. On the left, a hologram: the frame cut
        into stacked planes that slide against each other as you move. On the
        right, a point cloud: every dot sitting at the true depth the
        photograph holds, baked from its depth pass. One fakes relief
        beautifully; the other actually has it.
      </p>
      <p class="text-sm opacity-60 max-w-2xl">
        Sweep across a plate to orbit it; release mid-turn and it carries,
        then settles. On a phone, the clouds roll with the page. Reduced
        motion holds a still frame.
      </p>
    </div>
  </section>

  {ROWS.map((row, i) => (
    <section class={`py-10 sm:py-14 md:py-16 ${i % 2 === 1 ? 'section-light' : ''}`}>
      <div class="container-wide">
        <p class="eyebrow mb-2">{row.label}</p>
        <p class="text-sm opacity-55 mb-8 max-w-xl">{row.note}</p>
        <div class="grid gap-10 md:grid-cols-2 md:gap-12 items-start">
          <div>
            <KineticPlate category={row.id} />
            <p class="plate-tag">Hologram · stacked planes</p>
          </div>
          <div class="study-3d">
            <KineticPlate3D category={row.id} />
            <p class="plate-tag">Point cloud · true depth</p>
          </div>
        </div>
      </div>
    </section>
  ))}

  {/* ACT II - TEXTURE. Pixels a visitor can take (moved from the work page). */}
  <section class="py-12 sm:py-16 md:py-24 section-light">
    <div class="container-narrow mb-10 sm:mb-14">
      <h2 class="t-sub font-serif mb-3 sm:mb-4">Texture and Gaussian studies</h2>
      <p class="font-serif text-lg sm:text-xl text-cream/90 mb-3 max-w-xl">Take one home.</p>
      <p class="text-sm sm:text-base opacity-55 max-w-xl">
        The depth work above feeds these. Wallpapers, textures, and color palettes drawn from single frames, free to take. <a href="mailto:jndwcreative@gmail.com?subject=Beyond%20Photography" class="text-cream/80 underline underline-offset-4 hover:text-cream transition-colors duration-300">Inquire</a> for prints, licensing, or the full-resolution files.
      </p>
    </div>
    <div class="container-wide space-y-16 sm:space-y-24 md:space-y-28">
      <div class="reveal"><LiquidPlate src="img-6552" alt="Last light over a ridge at dusk" label="Sky" note="Last light, going down behind the ridge." index={0} /></div>
      <div class="reveal"><LiquidPlate src="7r51018-enhanced-sr" alt="Rain beaded across a dark green leaf" label="Earth" note="Rain on a leaf. The surface, up close." index={1} reverse /></div>
      <div class="reveal"><LiquidPlate src="dscf1022" alt="A wall combed into vertical ribs of light and shadow" label="Stone" note="A wall, raked into ribs of light." index={2} /></div>
    </div>
  </section>

  {/* ACT III - THE OBJECT. The relief, folded in whole: depth learned,
      pixels lifted, then the photograph cut as a surface you could hold. */}
  <ReliefStudies />

<style is:global>
  .plate-tag {
    margin-top: 1rem;
    font-size: 0.6875rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(244, 234, 222, 0.62);
  }
  /* If WebGL2 is unavailable the point cloud cannot mount; the hologram
     column carries the row alone rather than showing a dead figure. */
  .study-3d:has(.kplate3d[data-webgl="unsupported"]) { display: none; }
</style>
</BaseLayout>
